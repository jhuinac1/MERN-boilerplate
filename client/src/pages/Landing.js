import React, { Fragment } from 'react';

const Landing = () => (
    <Fragment>
        This is a MERN Boilerplate with a redux store and firebase connection for easy file uploading. 
    </Fragment>
    )

export default Landing;